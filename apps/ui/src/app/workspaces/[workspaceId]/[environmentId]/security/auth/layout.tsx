export const metadata = {
  title: 'VM-X AI Console - Security - Auth',
  description: 'VM-X AI Console - Security - Auth',
};

export default async function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
