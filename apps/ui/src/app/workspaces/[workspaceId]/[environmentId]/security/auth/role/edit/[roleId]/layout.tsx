export const metadata = {
  title: 'VM-X AI Console - Security - Auth - Edit Role',
  description: 'VM-X AI Console - Security - Auth - Edit Role',
};

export default async function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
