export const metadata = {
  title: 'VM-X AI Console - AI Resources',
  description: 'VM-X AI Console - AI Resources',
};

export default async function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
